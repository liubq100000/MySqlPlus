# MySqlPlus
Mybatis xml 的解析工具 ，转换为可执行Sql

用途：
Web项目可以把针对表的所有操作SQL，保存到数据库或文件中，网页动态加载修改，使用整个工具实时解析成SQL执行，
达到修改的自由，不需要IDE和重启。

实现：
采用Aviator进行if的表达式判断，自己直接写的解析xml，没有使用jdom,dom4j等工具，不是哪些工具不好，只是太大了，这个小工具没有必须要


性能：
解析xml大约耗时：20ms
执行表达式耗时：执行一个95ms，以后基本式1ms
